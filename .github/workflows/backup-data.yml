# DISABLED — SECURITY INCIDENT 2026-08-08.
#
# This workflow published full project-data snapshots (real client names, project details) to
# the `data-backups` branch of this PUBLIC repository, world-readable to anyone. Do not
# re-enable until the publish target is a private destination (a private companion repo, a
# private Supabase Storage bucket, S3 with restricted access, etc.) — never a branch of this
# repo, since this repo is public. See the incident notes for required cleanup steps
# (delete the `data-backups` branch; it cannot be done from this workflow).
name: Backup Data

on:
  workflow_dispatch: {}

permissions:
  contents: read

jobs:
  backup:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "::error::Backup workflow is disabled after a public data-exposure incident. See the comment at the top of this file before re-enabling."
          exit 1
